# The module is opt-in: a workspace can carry it without every app paying for it.
if(CONFIG_RUNTT)
  zephyr_library_named(runtt)
  zephyr_include_directories(${ZEPHYR_CURRENT_MODULE_DIR}/include)
  zephyr_library_sources_ifdef(CONFIG_RUNTT_CONFIRM_DEADLINE ../src/confirm.c)
  zephyr_library_sources_ifdef(CONFIG_RUNTT_HEALTH       ../src/health.c)
  zephyr_library_sources_ifdef(CONFIG_RUNTT_IDENTITY     ../src/identity.c)
  zephyr_library_sources_ifdef(CONFIG_RUNTT_USB          ../src/usbd.c)
  zephyr_library_sources_ifdef(CONFIG_RUNTT_SMP_DESCRIBE ../src/describe.c)
  zephyr_library_sources_ifdef(CONFIG_RUNTT_SMP_CAN      ../src/smp_can.c)
  zephyr_library_sources_ifdef(CONFIG_RUNTT_CAN_LOG      ../src/can_log.c)
endif()
